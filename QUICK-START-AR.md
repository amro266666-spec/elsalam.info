# 🚀 البداية السريعة - 3 دقائق فقط!

## ⚡ أسرع طريقة للنشر (Vercel)

### الخطوة 1: ارفع على GitHub
```bash
# في Terminal أو CMD:
git init
git add .
git commit -m "صفحة هبوط صناعية"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

### الخطوة 2: انشر على Vercel
1. اذهب لـ [vercel.com](https://vercel.com)
2. سجل دخول بحساب GitHub
3. اضغط "Import Project"
4. اختر repository الخاص بك
5. اضغط "Deploy"

**✅ خلاص! موقعك على الإنترنت في أقل من دقيقة!**

---

## 🎯 أو بدون GitHub (Netlify Drop)

```bash
# 1. بناء المشروع
npm install
npm run build

# 2. اذهب لـ
```
👉 [app.netlify.com/drop](https://app.netlify.com/drop)

**اسحب مجلد `dist` وأفلته** → انتهى!

---

## 📝 ربط النموذج بإيميلك (مجاناً)

### استخدم Formspree:

1. اذهب لـ [formspree.io](https://formspree.io)
2. سجل دخول مجاناً
3. أنشئ نموذج جديد
4. انسخ الـ endpoint

5. عدّل في `LeadFormSection.tsx`:

```typescript
const onSubmit = async (data: FormData) => {
  try {
    await fetch('https://formspree.io/f/YOUR_FORM_ID', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data),
    });
    
    alert('تم الإرسال! شكراً لتواصلك');
    reset();
  } catch (error) {
    alert('حدث خطأ، حاول مرة أخرى');
  }
};
```

**✅ الآن كل طلب سيوصلك على إيميلك!**

---

## 🔧 الأوامر المهمة

```bash
# تثبيت المكتبات
npm install

# بناء المشروع
npm run build

# معاينة محلية
npx serve -s dist
```

---

## 📱 اختبر الموقع

بعد النشر، اختبر:
- ✅ الموقع يفتح بدون مشاكل
- ✅ النموذج يشتغل
- ✅ responsive على الجوال
- ✅ النصوص العربية صحيحة

---

## 🆘 مشكلة؟

**404 Error بعد النشر؟**
- ✅ ملفات `vercel.json` و `netlify.toml` موجودة (تم إضافتها)

**النموذج لا يعمل؟**
- ✅ سجل في Formspree وحط endpoint الصحيح

**الصور لا تظهر؟**
- ✅ تأكد إنك عملت `npm run build` قبل الرفع

---

## 📄 المزيد من التفاصيل

شوف ملف `DEPLOYMENT-GUIDE-AR.md` لدليل شامل مع كل الخيارات!

---

**🎉 بالتوفيق في نشر موقعك!**
